## Folder for storing voiceprints after enrollment.

```bash
npm run enroll <enrollment file name here>
```

Then verify the stored voiceprint

```bash
npm run verify <stored voiceprint lable>
```

**If the print label is not provided, it will default to `voiceprint.print.json`**
